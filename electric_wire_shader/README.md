# Electric wire shader

![Screenshot](./screenshot.png?raw=true)

A shader that creates a stripe of light moving along a surface.
Works best as a next pass to a normal wire material (e.g. a `SpatialMaterial`).

